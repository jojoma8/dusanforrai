/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["www.istockphoto.com", "media.istockphoto.com"],
  },
};

module.exports = nextConfig;
